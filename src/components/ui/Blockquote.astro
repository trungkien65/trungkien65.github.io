---
/**
 * Blockquote: quote with left border.
 */
import { cn } from "@/lib/utils"

interface Props {
  class?: string
}

const { class: className = "" } = Astro.props
---
<blockquote
  class={cn("border-l-4 border-primary pl-4 italic text-muted-foreground", className)}
>
  <slot />
</blockquote>
